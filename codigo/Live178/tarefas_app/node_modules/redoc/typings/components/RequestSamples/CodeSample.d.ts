import * as React from 'react';
import { MediaContentModel } from '../..';
import { OperationModel, Unstable_ExternalCodeSample } from '../../services';
interface CodeSamplesProps {
    content?: MediaContentModel;
    operation: OperationModel;
    lang: string;
    source?: string;
    externalSample?: Unstable_ExternalCodeSample;
}
export default class CodeSample extends React.Component<CodeSamplesProps> {
    state: {
        activeIdx: number;
    };
    switchMedia: ({ idx }: {
        idx: any;
    }) => void;
    private renderDropdown;
    renderSample(exampleName?: string): JSX.Element;
    render(): JSX.Element;
}
export {};
